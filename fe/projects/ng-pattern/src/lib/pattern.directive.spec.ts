import { PatternDirective } from './pattern.directive';

describe('PatternDirective', () => {
  it('should create an instance', () => {
    const directive = new PatternDirective();
    expect(directive).toBeTruthy();
  });
});
